// Urls for the API and it's various endpoints

const BASE_URL = "https://api.noroff.dev/api/v1/holidaze";

const AUTH_URL = `${BASE_URL}/auth`;
export const LOGIN_URL = `${AUTH_URL}/login`;
export const REGISTER_URL = `${AUTH_URL}/register`;

export const PROFILES_URL = `${BASE_URL}/profiles`;

export const VENUES_URL = `${BASE_URL}/venues`;

export const BOOKINGS_URL = `${BASE_URL}/bookings`;
